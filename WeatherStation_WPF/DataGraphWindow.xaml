<Window x:Class="WeatherStation_WPF.DataGraphWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:DV="clr-namespace:System.Windows.Controls.DataVisualization;assembly=System.Windows.Controls.DataVisualization.Toolkit"
        xmlns:DVC="clr-namespace:System.Windows.Controls.DataVisualization.Charting;assembly=System.Windows.Controls.DataVisualization.Toolkit"
        Title="DataLogger" Height="480" Width="800">
    <Grid Margin="0,0,-6.2,-3.2" Background="#FFB7BB12">
        <Grid.RowDefinitions>
            <RowDefinition Height="100*" />
            <RowDefinition Height="100*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100*" />
            <ColumnDefinition Width="100*" />
        </Grid.ColumnDefinitions>
        
        <DVC:Chart Grid.Row="0" Grid.Column="0" Canvas.Top="80" Canvas.Left="10" Name="mcChart" Background="RosyBrown"  Loaded="mcChart_Loaded">
            <DVC:Chart.Series>
                <DVC:LineSeries Title="1"
                    IndependentValueBinding="{Binding Path=Key}"
                    DependentValueBinding="{Binding Path=Value}">
                </DVC:LineSeries>
            </DVC:Chart.Series>
        </DVC:Chart>

        <DVC:Chart Grid.Row="0" Grid.Column="1" Canvas.Top="80" Canvas.Left="10" Name="mcChart1" Background="Bisque"  Loaded="mcChart_Loaded">
            <DVC:Chart.Series>
                <DVC:PieSeries Title="Największe wyszukiwarki"
                    IndependentValueBinding="{Binding Path=Key}"
                    DependentValueBinding="{Binding Path=Value}">
                </DVC:PieSeries>
            </DVC:Chart.Series>
        </DVC:Chart>

        <DVC:Chart Grid.Row="1" Grid.Column="0" Canvas.Top="80" Canvas.Left="10" Name="mcChart2" Background="DarkBlue"  Loaded="mcChart_Loaded">
            <DVC:Chart.Series>
                <DVC:PieSeries Title="Największe wyszukiwarki"
                    IndependentValueBinding="{Binding Path=Key}"
                    DependentValueBinding="{Binding Path=Value}">
                </DVC:PieSeries>
            </DVC:Chart.Series>
        </DVC:Chart>

        <DVC:Chart Grid.Row="1" Grid.Column="1" Canvas.Top="80" Canvas.Left="10" Name="mcChart3" Background="Cyan"  Loaded="mcChart_Loaded">
            <DVC:Chart.Series>
                <DVC:AreaSeries Title="XXX"
                    VerticalAlignment="Stretch" 
                    HorizontalAlignment="Stretch" 
                    ItemsSource="{Binding Path=MemoryStats}" 
                    IndependentValuePath="Timestamp" 
                    DependentValuePath="ByteCount">
                </DVC:AreaSeries>
            </DVC:Chart.Series>
        </DVC:Chart>

    </Grid>
</Window>
